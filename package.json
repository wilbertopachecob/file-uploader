{
  "name": "file-uploader",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "description": "Root scripts to run server and frontend",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix front_end install",
    "start": "npm --prefix server run start",
    "build": "npm --prefix front_end run build",
    "dev": "concurrently \"npm --prefix server run start\" \"npm --prefix front_end run serve\"",
    "typecheck": "concurrently \"npm --prefix front_end run typecheck\" \"npm --prefix server run typecheck\"",
    "lint": "eslint --ext .js,.ts,.vue front_end/src server --max-warnings 0",
    "lint:fix": "eslint --ext .js,.ts,.vue front_end/src server --fix",
    "stop": "bash ./scripts/stop.sh"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.41.0",
    "@typescript-eslint/parser": "^8.39.0",
    "concurrently": "^9.2.0",
    "eslint": "^9.33.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-vue": "^10.4.0"
  }
}
